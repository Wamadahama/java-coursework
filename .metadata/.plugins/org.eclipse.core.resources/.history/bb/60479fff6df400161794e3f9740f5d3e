package lab3;

public class AckerFunction {
	private int spaces = 0;
	private int numberOfInvocations = 0;
	
	// getter for data field "numberOfInvocations"
	public int countOfInvocations() {
		return numberOfInvocations;
	}
	
	public int acker(int m, int n) {
		
		// The function has been called so increment it 
		++numberOfInvocations;

		// Print out enter stack trace 
		printSpaces();
		System.out.print("Enter Method Acker: m = " + m + " n = " + n + "\n");

		spaces += 4;

		int result = 0;
		
		
		// Implemtnation of the piecewise function in the lab pdf 
		if(m == 0)
			result = n+=1;
		else if(n == 0) {
			result = acker(m-1, 1);
		} else {
			result = acker(m-1, acker(m,n-1));
		}
		
		spaces -= 4; 
		
		printSpaces(); 
		System.out.print("Leave Method Acker: acker(" + m + ", " + n + ") = " + result + "\n");
		
		
		return result;
	}
	
	private void printSpaces() {
		for(int i = 0; i < spaces; i++)
			System.out.print(" ");
	}
}
